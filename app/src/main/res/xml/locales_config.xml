<?xml version="1.0" encoding="utf-8"?>
<!-- Languages offered for this app in Android's per-app language settings (Android 13+). Arabic is
     left out: it only translates the disguise strings. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="pt-BR" />
</locale-config>
